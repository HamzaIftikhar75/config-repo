server:
  port: 8080

spring:
  application:
    name: employee-service
  mongodb:
    uri: mongodb://localhost:27017/crud1
  data:
    mongodb:
      uri: mongodb://localhost:27017/crud1     # ← Add this as backup

logging:
  file:
    name: logs/employee-service.log
  level:
    org.springframework.data.mongodb: DEBUG
    org.mongodb.driver: INFO