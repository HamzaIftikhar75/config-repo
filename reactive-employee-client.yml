spring:
  application:
    name: reactive-employee-client
server:
  port: 8086   # choose a free port

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true